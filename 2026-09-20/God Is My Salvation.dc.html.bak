<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script src="./support.js"></script>
</head>
<body>
<x-dc>
<helmet>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<style>
html,body{margin:0;height:100%;background:#040814;overflow:hidden}
a{color:#d9b45f}a:hover{color:#f3dc9a}
@keyframes kenburns{from{transform:scale(1) translate(0,0)}to{transform:scale(1.14) translate(-2.5%,1.5%)}}
@keyframes grain{0%{transform:translate(0,0)}20%{transform:translate(-2%,1%)}40%{transform:translate(1%,-2%)}60%{transform:translate(-1%,-1%)}80%{transform:translate(2%,2%)}100%{transform:translate(0,0)}}
@keyframes emblem{0%,100%{box-shadow:0 0 22px 6px rgba(243,220,154,.35),0 0 60px 20px rgba(217,180,95,.18)}50%{box-shadow:0 0 34px 10px rgba(243,220,154,.6),0 0 90px 30px rgba(217,180,95,.3)}}
@keyframes breathe{0%,100%{opacity:.7}50%{opacity:1}}
</style>
</helmet>
<div ref="{{viewportRef}}" style="position:fixed;inset:0;background:#040814;overflow:hidden">
<div ref="{{stageRef}}" style="position:absolute;left:50%;top:50%;width:1920px;height:1080px;margin:-540px 0 0 -960px;transform-origin:center center;overflow:hidden;background:#070d1f;font-family:'Cormorant Garamond',Cormorant,Georgia,'Times New Roman',serif;color:#f0e2c2;user-select:none;cursor:default">

<canvas ref="{{skyRef}}" width="960" height="540" style="position:absolute;left:0;top:0;width:1920px;height:1080px;opacity:{{skyOpacity}};transition:opacity 1.6s ease"></canvas>

<div ref="{{wheelRef}}" style="position:absolute;inset:0;will-change:transform">
  <sc-for list="{{wedges}}" as="w" hint-placeholder-count="5">
    <div onClick="{{w.onClick}}" onMouseEnter="{{w.onEnter}}" onMouseLeave="{{w.onLeave}}" style="position:absolute;inset:0;clip-path:{{w.clip}};transform-origin:960px 540px;transform:{{w.transform}};filter:{{w.filter}};opacity:{{w.opacity}};z-index:{{w.z}};transition:{{w.transition}};cursor:pointer;will-change:transform">
      <canvas ref="{{w.canvasRef}}" width="960" height="540" style="position:absolute;left:0;top:0;width:1920px;height:1080px;display:block"></canvas>
      <div style="position:absolute;inset:0;background:radial-gradient(circle at 960px 540px,rgba(7,13,31,.82) 0,rgba(7,13,31,.42) 240px,rgba(7,13,31,0) 720px);opacity:{{w.tint}};transition:opacity 1.4s ease"></div>
    </div>
  </sc-for>
  <sc-for list="{{wedges}}" as="w" hint-placeholder-count="5">
    <div style="position:absolute;inset:0;pointer-events:none;transform-origin:960px 540px;transform:{{w.transform}};opacity:{{w.labelOpacity}};transition:{{w.transition}},opacity .5s ease;z-index:4">
      <div style="position:absolute;left:{{w.lx}}px;top:{{w.ly}}px;width:470px;margin-left:-235px;transform:translateY(-50%);text-align:center;padding:40px 20px;border-radius:50%;background:radial-gradient(ellipse at center,rgba(7,13,31,.72) 0,rgba(7,13,31,.45) 45%,rgba(7,13,31,0) 75%)">
        <div style="font-family:Inter,system-ui,sans-serif;font-size:19px;letter-spacing:.34em;color:#d9b45f;margin-bottom:12px">{{w.num}}</div>
        <div style="font-size:54px;line-height:1.04;font-weight:500;color:#f7e9c3;text-wrap:balance;text-shadow:0 2px 26px rgba(0,0,0,.75),0 0 44px rgba(217,180,95,.35)">{{w.title}}</div>
        <div style="font-family:Inter,system-ui,sans-serif;font-size:23px;font-weight:300;letter-spacing:.06em;color:#e8d8ab;margin-top:14px;text-shadow:0 2px 14px rgba(0,0,0,.8)">{{w.reference}}</div>
      </div>
    </div>
  </sc-for>
  <svg viewBox="0 0 1920 1080" style="position:absolute;left:0;top:0;width:1920px;height:1080px;pointer-events:none;z-index:5;opacity:{{linesOpacity}};transition:{{linesTransition}}">
    <sc-for list="{{wedges}}" as="w" hint-placeholder-count="5">
      <polygon points="{{w.points}}" fill="none" stroke="#d9b45f" stroke-width="1.4" style="opacity:.55"></polygon>
    </sc-for>
    <sc-for list="{{wedges}}" as="w" hint-placeholder-count="5">
      <polygon points="{{w.points}}" fill="none" stroke="#f3dc9a" stroke-linejoin="round" style="stroke-width:{{w.rimWidth}}px;opacity:{{w.rimOpacity}};stroke-dasharray:3400;stroke-dashoffset:{{w.rimDash}};transition:stroke-dashoffset 1.2s ease,opacity .7s ease,stroke-width .5s ease;filter:drop-shadow(0 0 6px rgba(243,220,154,.8))"></polygon>
    </sc-for>
    <circle cx="960" cy="540" r="138" fill="none" stroke="#d9b45f" stroke-width="1" style="opacity:.4"></circle>
  </svg>
</div>

<div ref="{{bloomRef}}" style="position:absolute;left:0;top:0;width:700px;height:700px;border-radius:50%;background:radial-gradient(circle,rgba(255,226,160,.22) 0,rgba(255,226,160,.06) 40%,rgba(255,226,160,0) 70%);mix-blend-mode:screen;pointer-events:none;z-index:6;opacity:{{bloomOpacity}};transition:opacity .8s ease"></div>

<div style="position:absolute;inset:0;pointer-events:none;z-index:20;opacity:{{sceneOpacity}};transition:{{sceneTransition}}">
  <div style="position:absolute;inset:0;pointer-events:{{scenePointer}}">
    <div style="position:absolute;left:0;right:0;top:0;height:420px;background:linear-gradient(180deg,rgba(4,8,20,.55),rgba(4,8,20,0));pointer-events:none"></div>
    <div style="position:absolute;left:50%;top:50%;transform:{{bigTitleTransform}};opacity:{{bigTitleOpacity}};transition:transform 1.3s cubic-bezier(.7,0,.18,1),opacity 1s ease;font-size:132px;font-weight:500;line-height:1;white-space:nowrap;color:#f6e3ad;text-shadow:0 0 30px rgba(243,220,154,.55),0 0 90px rgba(217,180,95,.35),0 3px 0 rgba(0,0,0,.4)">{{sceneTitle}}</div>
    <div style="position:absolute;left:80px;top:54px;transform-origin:left top;transform:{{smallTitleTransform}};opacity:{{smallTitleOpacity}};transition:transform 1.1s cubic-bezier(.7,0,.18,1),opacity .9s ease .1s">
      <div style="font-size:46px;font-weight:500;color:#f6e3ad;line-height:1;text-shadow:0 2px 20px rgba(0,0,0,.7)">{{sceneTitle}}</div>
      <div style="font-family:Inter,system-ui,sans-serif;font-size:21px;font-weight:300;letter-spacing:.1em;color:#d9b45f;margin-top:10px">{{sceneRef}}</div>
    </div>
    <div style="position:absolute;left:50%;top:140px;width:1440px;margin-left:-720px;text-align:center;transform:{{scriptureTransform}};opacity:{{scriptureOpacity}};transition:transform 1.2s cubic-bezier(.22,.8,.2,1),opacity 1.2s ease">
      <div style="font-size:46px;font-style:italic;font-weight:500;line-height:1.28;color:#f8ecc8;text-wrap:pretty;text-shadow:0 1px 0 rgba(255,245,210,.35),0 -1px 0 rgba(0,0,0,.7),0 0 26px rgba(243,220,154,.45),0 0 70px rgba(217,180,95,.28)">{{sceneScripture}}</div>
      <div style="font-family:Inter,system-ui,sans-serif;font-size:22px;letter-spacing:.28em;text-transform:uppercase;color:#d9b45f;margin-top:22px;text-shadow:0 0 18px rgba(217,180,95,.5)">{{sceneScriptureRef}}</div>
    </div>

    <sc-for list="{{questions}}" as="q" hint-placeholder-count="4">
      <div onClick="{{q.onClick}}" style="position:absolute;left:0;top:0;width:{{q.w}}px;transform-origin:center center;transform:{{q.transform}};opacity:{{q.opacity}};filter:{{q.filter}};z-index:{{q.z}};transition:{{q.transition}};cursor:pointer">
        <div style="position:relative;padding:34px 38px 36px;border-radius:20px;background:linear-gradient(160deg,rgba(18,28,58,.5),rgba(8,14,32,.42));backdrop-filter:blur(16px) saturate(1.2);-webkit-backdrop-filter:blur(16px) saturate(1.2);border:1px solid rgba(217,180,95,.6);box-shadow:inset 0 0 0 1px rgba(243,220,154,.12),inset 0 1px 0 rgba(255,240,200,.18),0 30px 70px rgba(0,0,0,.4);transition:transform .5s cubic-bezier(.22,.8,.2,1),background .5s ease,border-color .5s ease,box-shadow .5s ease" style-hover="transform:translateY(-10px);background:linear-gradient(160deg,rgba(30,44,84,.62),rgba(14,22,48,.55));border-color:rgba(243,220,154,.95);box-shadow:inset 0 0 0 1px rgba(243,220,154,.25),0 0 40px rgba(217,180,95,.25),0 40px 80px rgba(0,0,0,.45)">
          <div style="font-family:Inter,system-ui,-apple-system,'Segoe UI',sans-serif;font-size:42px;font-weight:300;line-height:1.28;color:#f6efdc;text-wrap:pretty;letter-spacing:-.005em">{{q.text}}</div>
          <div style="position:absolute;right:-16px;top:-16px;width:48px;height:48px;border-radius:50%;background:#0b1430;border:1.5px solid #d9b45f;color:#f3dc9a;font-size:28px;line-height:46px;text-align:center;box-shadow:0 0 18px rgba(217,180,95,.5);opacity:{{q.checkOpacity}};transition:opacity .6s ease">✓</div>
        </div>
      </div>
    </sc-for>

    <div style="position:absolute;left:80px;bottom:64px;display:flex;gap:40px;z-index:8">
      <div onClick="{{onStory}}" style="display:flex;flex-direction:column;align-items:center;gap:12px;cursor:pointer">
        <div style="width:78px;height:78px;border-radius:50%;border:1.5px solid {{storyBorder}};background:rgba(8,14,32,.55);color:#f3dc9a;font-size:34px;line-height:76px;text-align:center;box-shadow:0 0 22px rgba(217,180,95,{{storyGlow}}),inset 0 0 18px rgba(217,180,95,.15);transition:box-shadow .5s ease,border-color .5s ease,transform .4s ease" style-hover="transform:scale(1.06)">✦</div>
        <div style="font-family:Inter,system-ui,sans-serif;font-size:19px;letter-spacing:.3em;text-transform:uppercase;color:#e8d8ab">Story</div>
      </div>
      <div onClick="{{onToday}}" style="display:{{todayDisplay}};flex-direction:column;align-items:center;gap:12px;cursor:pointer">
        <div style="width:78px;height:78px;border-radius:50%;border:1.5px solid {{todayBorder}};background:rgba(8,14,32,.55);color:#f3dc9a;font-size:30px;line-height:76px;text-align:center;box-shadow:0 0 22px rgba(217,180,95,{{todayGlow}}),inset 0 0 18px rgba(217,180,95,.15);transition:box-shadow .5s ease,border-color .5s ease,transform .4s ease" style-hover="transform:scale(1.06)">◐</div>
        <div style="font-family:Inter,system-ui,sans-serif;font-size:19px;letter-spacing:.3em;text-transform:uppercase;color:#e8d8ab">Today</div>
      </div>
    </div>

    <div style="position:absolute;left:0;right:0;bottom:0;height:540px;transform:{{panelTransform}};transition:transform 1s cubic-bezier(.7,0,.18,1);z-index:7;background:linear-gradient(180deg,rgba(6,10,26,.86),rgba(4,7,18,.94));backdrop-filter:blur(22px);-webkit-backdrop-filter:blur(22px);border-top:1px solid rgba(217,180,95,.6);box-shadow:0 -30px 80px rgba(0,0,0,.5)">
      <div style="position:absolute;left:100px;right:100px;top:48px;bottom:48px;display:grid;grid-template-columns:600px minmax(0,1fr);gap:72px;align-items:center">
        <div style="position:relative;height:420px;padding:14px;border:2px solid rgba(217,180,95,.85);outline:1px solid rgba(217,180,95,.4);outline-offset:6px;background:#0a1128;box-shadow:0 0 0 1px rgba(243,220,154,.12) inset,0 30px 60px rgba(0,0,0,.5)">
          <div style="position:absolute;left:-7px;top:-7px;width:12px;height:12px;background:#0a1128;border:1.5px solid #f3dc9a;transform:rotate(45deg)"></div>
          <div style="position:absolute;right:-7px;top:-7px;width:12px;height:12px;background:#0a1128;border:1.5px solid #f3dc9a;transform:rotate(45deg)"></div>
          <div style="position:absolute;left:-7px;bottom:-7px;width:12px;height:12px;background:#0a1128;border:1.5px solid #f3dc9a;transform:rotate(45deg)"></div>
          <div style="position:absolute;right:-7px;bottom:-7px;width:12px;height:12px;background:#0a1128;border:1.5px solid #f3dc9a;transform:rotate(45deg)"></div>
          <div style="position:relative;width:100%;height:100%;overflow:hidden;border:1px solid rgba(217,180,95,.45)">
            <sc-if value="{{panelHasImage}}" hint-placeholder-val="{{false}}">
              <div style="width:100%;height:100%;background-image:url({{panelImage}});background-size:cover;background-position:center;animation:kenburns 26s ease-in-out infinite alternate"></div>
            </sc-if>
            <sc-if value="{{panelNoImage}}" hint-placeholder-val="{{true}}">
              <div style="position:absolute;inset:0;background:repeating-linear-gradient(135deg,rgba(217,180,95,.14) 0 16px,rgba(217,180,95,.04) 16px 32px),#111d3d;animation:kenburns 26s ease-in-out infinite alternate"></div>
              <div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;padding:30px;text-align:center;font-family:ui-monospace,Menlo,Consolas,monospace;font-size:17px;letter-spacing:.06em;color:rgba(243,220,154,.75)">{{panelImageLabel}}</div>
            </sc-if>
          </div>
        </div>
        <div style="min-width:0">
          <div style="font-family:Inter,system-ui,sans-serif;font-size:19px;letter-spacing:.32em;text-transform:uppercase;color:#d9b45f;margin-bottom:16px">{{panelKicker}}</div>
          <div style="font-size:52px;font-weight:500;line-height:1.05;color:#f6e3ad;margin-bottom:28px;text-shadow:0 0 24px rgba(217,180,95,.3)">{{panelHeading}}</div>
          <div style="display:flex;flex-direction:column;gap:18px">
            <sc-for list="{{panelParas}}" as="p" hint-placeholder-count="3">
              <div style="font-family:Inter,system-ui,sans-serif;font-size:29px;font-weight:300;line-height:1.42;color:#ece2c8;text-wrap:pretty;opacity:{{p.opacity}};transition:{{p.transition}}">{{p.text}}</div>
            </sc-for>
          </div>
        </div>
      </div>
    </div>

    <div style="position:absolute;right:72px;bottom:64px;width:520px;padding:26px 30px;border-radius:14px;background:rgba(6,10,26,.82);border:1px solid rgba(217,180,95,.45);backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);opacity:{{notesOpacity}};transform:{{notesTransform}};transition:opacity .6s ease,transform .6s cubic-bezier(.22,.8,.2,1);z-index:9;pointer-events:none">
      <div style="font-family:Inter,system-ui,sans-serif;font-size:15px;letter-spacing:.3em;text-transform:uppercase;color:#d9b45f;margin-bottom:10px">Teacher note</div>
      <div style="font-family:Inter,system-ui,sans-serif;font-size:24px;font-weight:300;line-height:1.4;color:#ece2c8">{{notesText}}</div>
    </div>
  </div>
</div>

<div onClick="{{onHome}}" style="position:absolute;left:50%;top:50%;width:250px;height:250px;border-radius:50%;transform:{{centerTransform}};opacity:{{centerOpacity}};transition:transform 1.4s cubic-bezier(.7,0,.18,1),opacity 1s ease;z-index:30;cursor:pointer;background:radial-gradient(circle at 50% 40%,#132048 0,#0a1128 70%,#070d1f 100%);border:1.5px solid rgba(217,180,95,.9);box-shadow:0 0 0 10px rgba(7,13,31,.9),0 0 0 11px rgba(217,180,95,.35),0 0 80px rgba(217,180,95,.25),inset 0 0 40px rgba(217,180,95,.12);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:18px" style-hover="border-color:#f3dc9a;box-shadow:0 0 0 10px rgba(7,13,31,.9),0 0 0 11px rgba(243,220,154,.7),0 0 110px rgba(217,180,95,.45),inset 0 0 40px rgba(217,180,95,.2)">
  <div style="width:22px;height:22px;border-radius:50%;background:radial-gradient(circle,#fff6d6 0,#f3dc9a 45%,#d9b45f 100%);animation:emblem 4s ease-in-out infinite"></div>
  <div style="font-size:42px;font-weight:500;line-height:1;color:#f6e3ad;text-shadow:0 0 20px rgba(217,180,95,.4);white-space:nowrap">{{hubTitle}}</div>
</div>

<div onClick="{{onStart}}" style="position:absolute;inset:0;z-index:40;display:flex;flex-direction:column;align-items:center;justify-content:center;transform-origin:center 45%;transform:{{openingTransform}};opacity:{{openingOpacity}};pointer-events:{{openingPointer}};transition:transform 1.6s cubic-bezier(.7,0,.18,1),opacity 1.2s ease .2s;cursor:pointer">
  <div style="font-size:150px;font-weight:500;line-height:1;color:#f8e6b3;letter-spacing:.01em;opacity:{{openOn}};transform:translateY({{openTitleY}}px);transition:opacity 2.4s ease .3s,transform 2.4s cubic-bezier(.22,.8,.2,1) .3s;text-shadow:0 0 40px rgba(243,220,154,.5),0 0 120px rgba(217,180,95,.35),0 4px 0 rgba(0,0,0,.35)">{{mainTitle}}</div>
  <div style="width:180px;height:1px;background:linear-gradient(90deg,rgba(217,180,95,0),#d9b45f,rgba(217,180,95,0));margin:44px 0 40px;opacity:{{openOn}};transition:opacity 1.6s ease 1.6s"></div>
  <div style="display:flex;flex-direction:column;align-items:center;gap:10px;font-size:48px;font-style:italic;line-height:1.25;color:#f1e3bf;text-align:center;text-shadow:0 0 22px rgba(243,220,154,.35),0 2px 0 rgba(0,0,0,.4)">
    <div style="opacity:{{openOn}};transition:opacity 1.6s ease 2.4s">Behold, God is my salvation;</div>
    <div style="opacity:{{openOn}};transition:opacity 1.6s ease 3.4s">I will trust, and not be afraid:</div>
    <div style="opacity:{{openOn}};transition:opacity 1.6s ease 4.4s">for the LORD JEHOVAH is my strength and my song;</div>
    <div style="opacity:{{openOn}};transition:opacity 1.6s ease 5.4s">he also is become my salvation.</div>
    <div style="font-family:Inter,system-ui,sans-serif;font-style:normal;font-size:20px;letter-spacing:.3em;text-transform:uppercase;color:#d9b45f;margin-top:18px;opacity:{{openOn}};transition:opacity 1.6s ease 6.4s">Isaiah 12:2</div>
  </div>
  <div style="position:absolute;bottom:70px;font-family:Inter,system-ui,sans-serif;font-size:20px;letter-spacing:.24em;text-transform:uppercase;color:rgba(232,216,171,.75);opacity:{{openHintOn}};transition:opacity 1.4s ease;animation:breathe 3s ease-in-out infinite">Press Enter or click to begin</div>
</div>

<div style="position:absolute;inset:0;z-index:50;opacity:{{closingOpacity}};pointer-events:{{closingPointer}};transition:opacity 1.6s ease;display:flex;flex-direction:column;align-items:center;justify-content:center">
  <canvas ref="{{closeSkyRef}}" width="960" height="540" style="position:absolute;left:0;top:0;width:1920px;height:1080px"></canvas>
  <div style="position:absolute;inset:0;background:radial-gradient(ellipse at 50% 60%,rgba(0,0,0,0) 30%,rgba(10,6,14,.55) 100%)"></div>
  <div style="position:relative;display:flex;flex-direction:column;align-items:center;gap:14px;font-size:84px;font-weight:500;line-height:1.2;color:#fff3d0;text-align:center;text-shadow:0 1px 0 rgba(255,245,210,.4),0 -1px 0 rgba(60,20,0,.6),0 0 30px rgba(255,220,150,.55),0 0 100px rgba(255,190,90,.35)">
    <div>Behold, God is my salvation;</div>
    <div>I will trust, and not be afraid:</div>
    <div>for the LORD JEHOVAH is my strength and my song;</div>
    <div>he also is become my salvation.</div>
    <div style="font-family:Inter,system-ui,sans-serif;font-size:26px;letter-spacing:.34em;text-transform:uppercase;color:#ffe7a8;margin-top:34px;text-shadow:0 0 20px rgba(255,200,100,.6)">Isaiah 12:2</div>
  </div>
</div>

<div style="position:absolute;right:290px;top:70px;z-index:31;font-family:Inter,system-ui,sans-serif;font-size:34px;font-weight:300;letter-spacing:.12em;font-variant-numeric:tabular-nums;color:{{timerColor}};opacity:{{timerOpacity}};transition:opacity .6s ease;text-shadow:0 0 16px rgba(217,180,95,.4);pointer-events:none;display:flex;align-items:center;gap:14px">
  <div style="width:8px;height:8px;border-radius:50%;background:#d9b45f;animation:breathe 2s ease-in-out infinite"></div>
  <div>{{timerText}}</div>
</div>

<div style="position:absolute;left:0;right:0;bottom:18px;z-index:32;text-align:center;font-family:Inter,system-ui,sans-serif;font-size:21px;font-weight:300;letter-spacing:.14em;color:rgba(232,216,171,.72);opacity:{{hintOpacity}};transition:opacity 1.4s ease;pointer-events:none">1–5 scenes &nbsp;·&nbsp; Esc back &nbsp;·&nbsp; N notes &nbsp;·&nbsp; T timer &nbsp;·&nbsp; B blackout &nbsp;·&nbsp; C closing</div>

<canvas ref="{{dustRef}}" width="960" height="540" style="position:absolute;left:0;top:0;width:1920px;height:1080px;pointer-events:none;z-index:60;mix-blend-mode:screen"></canvas>
<div style="position:absolute;inset:0;pointer-events:none;z-index:61;background:radial-gradient(ellipse at center,rgba(0,0,0,0) 42%,rgba(3,6,16,.6) 100%)"></div>
<div style="position:absolute;inset:0;pointer-events:none;z-index:61;mix-blend-mode:screen;background:radial-gradient(ellipse at 50% -12%,rgba(255,230,170,.15),rgba(255,230,170,0) 60%)"></div>
<div ref="{{grainRef}}" style="position:absolute;left:-50%;top:-50%;width:200%;height:200%;pointer-events:none;z-index:62;mix-blend-mode:overlay;opacity:{{grainOpacity}};background-size:160px 160px;animation:grain .7s steps(5) infinite"></div>

<div style="position:absolute;inset:0;background:#000;z-index:90;opacity:{{blackoutOpacity}};pointer-events:{{blackoutPointer}};transition:opacity .6s ease"></div>
</div>
</div>
</x-dc>
<script type="text/x-dc" data-dc-script data-props="{&quot;particleDensity&quot;:{&quot;editor&quot;:&quot;range&quot;,&quot;default&quot;:1,&quot;min&quot;:0.3,&quot;max&quot;:1.6,&quot;step&quot;:0.1,&quot;tsType&quot;:&quot;number&quot;,&quot;section&quot;:&quot;Motion&quot;},&quot;transitionSpeed&quot;:{&quot;editor&quot;:&quot;range&quot;,&quot;default&quot;:1,&quot;min&quot;:0.6,&quot;max&quot;:1.8,&quot;step&quot;:0.1,&quot;unit&quot;:&quot;×&quot;,&quot;tsType&quot;:&quot;number&quot;,&quot;section&quot;:&quot;Motion&quot;},&quot;grain&quot;:{&quot;editor&quot;:&quot;boolean&quot;,&quot;default&quot;:true,&quot;tsType&quot;:&quot;boolean&quot;,&quot;section&quot;:&quot;Atmosphere&quot;}}">
class Component extends DCLogic {
  // ============================================================
  // CONTENT CONFIG — all text and image paths live here.
  // Set story/today.image to a path (e.g. "images/snow.jpg") to replace the placeholder frame.
  // ============================================================
  static CONFIG = {
    title: "God Is My Salvation",
    hub: "Isaiah 1–12",
    scenes: [
      {
        title: "Scarlet to Snow", reference: "Isaiah 1:11–18",
        scripture: "Come now, and let us reason together, saith the LORD: though your sins be as scarlet, they shall be as white as snow; though they be red like crimson, they shall be as wool.",
        scriptureRef: "Isaiah 1:18",
        story: { heading: "The stain that would not wash out", image: null, imageLabel: "drop image · scarlet dye / fresh snow",
          paragraphs: [
            "In Isaiah's day, scarlet dye came from crushed insects and was prized because it would not wash out.",
            "The Lord chose the most permanent stain people knew and promised to make it white as snow.",
            "In verses 11–15, He says He is weary of sacrifices offered without any change of heart." ] },
        today: null,
        questions: [
          "Why picture sin as a stain that can't wash out?",
          "What turns the sacrament from routine into covenant?",
          "Why do we think our own sin is the exception?",
          "\u201cLet us reason together\u201d — what does that say about God?" ],
        note: "Warm-up scene. Personal and low-risk."
      },
      {
        title: "Here Am I, Send Me", reference: "Isaiah 6; Abraham 3:27",
        scripture: "Also I heard the voice of the Lord, saying, Whom shall I send, and who will go for us? Then said I, Here am I; send me.",
        scriptureRef: "Isaiah 6:8",
        story: { heading: "A live coal, then a volunteer", image: null, imageLabel: "drop image · seraph and live coal",
          paragraphs: [
            "Isaiah sees the Lord and cries that he is \u201cundone,\u201d a man of unclean lips.",
            "A seraph touches his lips with a live coal. Only after that cleansing does he volunteer.",
            "The premortal Christ answered with the same words (Abraham 3:27)." ] },
        today: { heading: "88,500 missionaries", image: null, imageLabel: "drop image · missionaries",
          paragraphs: [
            "In July 2026 the Church reached about 88,500 full-time missionaries, the most in its history.",
            "The surge came largely from the first wave of 18-year-old sister missionaries.",
            "President Oaks said missionary work is \u201csoaring to new heights.\u201d" ] },
        questions: [
          "Why does cleansing come before the call?",
          "What would you tell an 18-year-old who feels unready?",
          "When have you felt \u201cundone\u201d by an assignment?",
          "What does \u201cHere am I\u201d look like without a missionary tag?" ],
        note: "Invite returned missionaries and parents of missionaries ahead of time."
      },
      {
        title: "Unto Us a Child Is Born", reference: "Isaiah 7:14; 9:2, 6",
        scripture: "For unto us a child is born, unto us a son is given: and the government shall be upon his shoulder: and his name shall be called Wonderful, Counsellor, The mighty God, The everlasting Father, The Prince of Peace.",
        scriptureRef: "Isaiah 9:6",
        story: { heading: "A sign for a king who wouldn't ask", image: null, imageLabel: "drop image · star over Bethlehem",
          paragraphs: [
            "Isaiah gave these prophecies about 700 years before Bethlehem, to King Ahaz, who refused to ask God for a sign (Isaiah 7:10–14).",
            "God gave him one anyway." ] },
        today: { heading: "Handel's Messiah, 1741", image: null, imageLabel: "drop image · Handel's Messiah score",
          paragraphs: [
            "In 1741 George Frideric Handel composed \u201cMessiah\u201d in about 24 days.",
            "Much of it sets Isaiah's words to music, and nearly three centuries later it is still performed worldwide every Christmas and Easter." ] },
        questions: [
          "Which name of Christ do you need most right now?",
          "When have you seen a great light in the dark?",
          "Why give a sign to a king who wouldn't ask?" ],
        note: "Play about 30 seconds of \u201cFor Unto Us a Child Is Born\u201d as this scene opens."
      },
      {
        title: "The Mountain of the Lord's House", reference: "Isaiah 2:2–3; 5:26",
        scripture: "And it shall come to pass in the last days, that the mountain of the LORD's house shall be established in the top of the mountains... and all nations shall flow unto it... and he will teach us of his ways, and we will walk in his paths.",
        scriptureRef: "Isaiah 2:2–3",
        story: { heading: "Ensign Peak, 1847", image: null, imageLabel: "drop image · Ensign Peak",
          paragraphs: [
            "Days after arriving in the Salt Lake Valley in 1847, Brigham Young and others climbed Ensign Peak, seeing in it Isaiah's \u201censign to the nations\u201d (Isaiah 5:26).",
            "See also the hymn \u201cHigh on the Mountain Top.\u201d" ] },
        today: { heading: "One million tickets in a day", image: null, imageLabel: "drop image · Salt Lake Temple",
          paragraphs: [
            "When reservations opened on September 1, 2026, the first 1 million free tickets to the Salt Lake Temple open house were claimed in under 24 hours — 250,000 of them in the first 30 minutes.",
            "It is the first time since 1893 the public can see inside, and about 5 million visitors are expected.",
            "During the renovation, crews lifted the temple to give it a new foundation." ] },
        questions: [
          "Why would millions want to go up to His house?",
          "What has the temple taught you that no class could?",
          "What in your foundation needs rebuilding?",
          "Change \u201cshall\u201d to \u201cis.\u201d What is already happening?" ],
        note: "Ask who got tickets. Quick, energizing opener."
      },
      {
        title: "The Peaceable Kingdom", reference: "Isaiah 11:6–9; 12:2",
        scripture: "Behold, God is my salvation; I will trust, and not be afraid: for the LORD JEHOVAH is my strength and my song; he also is become my salvation.",
        scriptureRef: "Isaiah 12:2",
        story: { heading: "A peace he did not yet have", image: null, imageLabel: "drop image · Peaceable Kingdom, Edward Hicks",
          paragraphs: [
            "When Moroni appeared to Joseph Smith in 1823, he quoted Isaiah 11 and said it was about to be fulfilled (JS–H 1:40).",
            "The Quaker painter Edward Hicks painted Isaiah 11:6 dozens of times while his own religious community was splitting apart. He painted a peace he did not yet have." ] },
        today: { heading: "Grand Blanc, Michigan", image: null, imageLabel: "drop image · Grand Blanc chapel",
          paragraphs: [
            "On September 28, 2025, a man drove his truck into a Latter-day Saint chapel in Grand Blanc Township, Michigan, opened fire, and set the building ablaze. Four people were killed.",
            "Lisa Louis, whose father was killed, later wrote that she forgave the gunman.",
            "Dave Butler, a Latter-day Saint in Utah with no tie to the ward, raised more than $333,000 for the gunman's widow and son, calling them victims too.",
            "The Church will rebuild on the same site, breaking ground September 28, 2026 — one year to the day." ] },
        questions: [
          "Is this what wolf and lamb dwelling together looks like?",
          "What makes it possible to rebuild where you were hurt?",
          "Why paint a peace you can't yet see?",
          "What gets us from fear to \u201cI will trust\u201d?" ],
        note: "Introduce Grand Blanc gently. Keep the focus on the response, not the attack. Strong closing scene; end by reading Isaiah 12:2 together."
      }
    ]
  };

  // Wedge geometry on the 1920×1080 stage (clockwise from top-left). 5 vertices each so clip-path can interpolate to full screen.
  static GEOM = [
    { clip: [[960,540],[0,228],[0,0],[960,0],[960,270]], full: [[1920,1080],[0,1080],[0,0],[1920,0],[1920,540]], lx: 737, ly: 233, angle: 234 },
    { clip: [[960,540],[960,0],[1920,0],[1920,228],[1440,384]], full: [[0,1080],[0,0],[1920,0],[1920,1080],[960,1080]], lx: 1183, ly: 233, angle: -54 },
    { clip: [[960,540],[1920,228],[1920,1080],[1352,1080],[1156,810]], full: [[0,0],[1920,0],[1920,1080],[960,1080],[0,1080]], lx: 1321, ly: 657, angle: 18 },
    { clip: [[960,540],[1352,1080],[960,1080],[568,1080],[764,810]], full: [[960,0],[1920,0],[1920,1080],[0,1080],[0,0]], lx: 960, ly: 930, angle: 90 },
    { clip: [[960,540],[568,1080],[0,1080],[0,228],[480,384]], full: [[1920,0],[1920,1080],[0,1080],[0,0],[960,0]], lx: 599, ly: 657, angle: 162 }
  ];

  constructor(props) {
    super(props);
    const C = Component.CONFIG;
    this.state = { phase: 'opening', openOn: 0, openHint: 0, assembled: false, inTransit: false, active: null, hover: null, visited: [], sceneUI: false, titleBig: false, qReady: false, qRisen: false, focusQ: null, discussed: {}, panel: null, notes: false, timerOn: false, timerLeft: 300, blackout: false, closing: false, hint: 0 };
    this.r = {}; ['viewport', 'stage', 'wheel', 'bloom', 'grain', 'sky', 'dust', 'closeSky'].forEach(k => this.r[k] = React.createRef());
    this.cv = C.scenes.map(() => React.createRef());
    this.k = C.scenes.map(() => 0.45); this.parts = {}; this.mouse = { x: 960, y: 540 }; this.par = { x: 0, y: 0 };
    this.scale = 1; this.transUntil = 0; this.skyUntil = 0; this.lastActive = 0; this.lastPanel = null; this.frame = 0; this.timers = [];
  }
  get D() { return 1.4 * (this.props.transitionSpeed ?? 1); }
  later(fn, ms) { const id = setTimeout(fn, ms); this.timers.push(id); return id; }

  componentDidMount() {
    this.fit(); window.addEventListener('resize', this.fit); window.addEventListener('keydown', this.onKey); window.addEventListener('mousemove', this.onMove);
    this.t0 = performance.now(); this.raf = requestAnimationFrame(this.loop);
    this.makeGrain();
    this.later(() => this.setState({ openOn: 1 }), 400);
    this.later(() => this.setState({ openHint: 1 }), 7500);
  }
  componentWillUnmount() {
    window.removeEventListener('resize', this.fit); window.removeEventListener('keydown', this.onKey); window.removeEventListener('mousemove', this.onMove);
    cancelAnimationFrame(this.raf); this.timers.forEach(clearTimeout); clearInterval(this.tick);
  }

  fit = () => { const v = this.r.viewport.current, s = this.r.stage.current; if (!v || !s) return; this.scale = Math.min(v.clientWidth / 1920, v.clientHeight / 1080); s.style.transform = `scale(${this.scale})`; };
  onMove = (e) => {
    const s = this.r.stage.current; if (!s) return; const rc = s.getBoundingClientRect();
    this.mouse = { x: (e.clientX - rc.left) / this.scale, y: (e.clientY - rc.top) / this.scale };
    const b = this.r.bloom.current; if (b) b.style.transform = `translate(${this.mouse.x - 350}px,${this.mouse.y - 350}px)`;
  };
  onKey = (e) => {
    const k = e.key, st = this.state;
    if (k === 'Enter' || k === ' ') { if (st.phase === 'opening') { e.preventDefault(); this.goHub(); } return; }
    if (/^[1-5]$/.test(k)) { const i = +k - 1; if (st.phase === 'opening') { this.goHub(); this.later(() => this.enterScene(i), this.D * 1000 + 600); } else if (st.active !== i && !st.closing) this.enterScene(i); return; }
    if (k === 'Escape') { this.back(); return; }
    const l = k.toLowerCase();
    if (l === 'b') this.setState({ blackout: !st.blackout });
    else if (l === 'c') this.setState({ closing: !st.closing });
    else if (l === 't') this.toggleTimer();
    else if (l === 'n') this.setState({ notes: !st.notes });
  };
  back = () => {
    const st = this.state;
    if (st.closing) return this.setState({ closing: false });
    if (st.blackout) return this.setState({ blackout: false });
    if (st.panel) return this.setState({ panel: null });
    if (st.focusQ != null) return this.setState({ focusQ: null });
    if (st.phase === 'scene') this.exitScene();
  };
  goHub = () => {
    if (this.state.phase !== 'opening') return;
    this.skyUntil = performance.now() + 2200; this.setState({ phase: 'hub' });
    this.later(() => this.setState({ assembled: true }), this.D * 1000 + 700);
    this.later(() => this.setState({ hint: 1 }), 2400);
    this.later(() => this.setState({ hint: 0 }), 12000);
  };
  enterScene = (i) => {
    const D = this.D * 1000; this.lastActive = i; this.transUntil = performance.now() + D + 300;
    const visited = this.state.visited.includes(i) ? this.state.visited : [...this.state.visited, i];
    this.setState({ phase: 'scene', active: i, hover: null, inTransit: true, visited, focusQ: null, panel: null, sceneUI: false, titleBig: true, qReady: false, qRisen: false, notes: false });
    this.later(() => this.setState({ sceneUI: true }), D * 0.4);
    this.later(() => this.setState({ inTransit: false }), D + 300);
    this.later(() => this.setState({ titleBig: false }), D + 600);
    this.later(() => this.setState({ qReady: true }), D + 800);
    this.later(() => this.setState({ qRisen: true }), D + 2200);
  };
  exitScene = () => {
    const D = this.D * 1000; this.transUntil = performance.now() + D + 300;
    this.setState({ phase: 'hub', active: null, inTransit: true, focusQ: null, panel: null, sceneUI: false, qReady: false, qRisen: false, titleBig: false });
    this.later(() => this.setState({ inTransit: false }), D + 300);
  };
  toggleQ = (qi) => {
    const { focusQ, discussed, active } = this.state;
    if (focusQ === qi) { const d = { ...discussed }; d[active] = [...new Set([...(d[active] || []), qi])]; this.setState({ focusQ: null, discussed: d }); }
    else this.setState({ focusQ: qi });
  };
  togglePanel = (p) => this.setState(s => ({ panel: s.panel === p ? null : p }));
  toggleTimer = () => {
    if (this.state.timerOn) { clearInterval(this.tick); this.setState({ timerOn: false, timerLeft: 300 }); return; }
    this.setState({ timerOn: true, timerLeft: 300 });
    this.tick = setInterval(() => this.setState(s => s.timerLeft <= 0 ? null : { timerLeft: s.timerLeft - 1 }), 1000);
  };
  makeGrain() {
    const c = document.createElement('canvas'); c.width = c.height = 160; const x = c.getContext('2d'); const d = x.createImageData(160, 160);
    for (let i = 0; i < d.data.length; i += 4) { const v = Math.random() * 255 | 0; d.data[i] = d.data[i + 1] = d.data[i + 2] = v; d.data[i + 3] = 255; }
    x.putImageData(d, 0, 0); const g = this.r.grain.current; if (g) g.style.backgroundImage = `url(${c.toDataURL()})`;
  }

  // ---------- render loop ----------
  loop = () => {
    this.raf = requestAnimationFrame(this.loop);
    const now = performance.now(), t = (now - this.t0) / 1000, st = this.state; this.frame++;
    if (st.blackout) return;
    const dens = this.props.particleDensity ?? 1;
    const tx = st.phase === 'hub' ? (this.mouse.x / 1920 - .5) * -24 : 0, ty = st.phase === 'hub' ? (this.mouse.y / 1080 - .5) * -16 : 0;
    this.par.x += (tx - this.par.x) * .06; this.par.y += (ty - this.par.y) * .06;
    const wh = this.r.wheel.current; if (wh) wh.style.transform = `translate(${this.par.x.toFixed(2)}px,${this.par.y.toFixed(2)}px)`;
    const ctxOf = r => { const c = r.current; return c ? c.getContext('2d') : null; };
    if (st.phase === 'opening' || now < this.skyUntil) { const c = ctxOf(this.r.sky); if (c) this.drawSky(c, 960, 540, t, 0); }
    if (st.closing) { const c = ctxOf(this.r.closeSky); if (c) this.drawSky(c, 960, 540, t + 30, 1); }
    const inTransit = now < this.transUntil;
    if (st.phase !== 'opening' && !(st.phase === 'hub' && !inTransit && this.frame % 2)) {
      Component.CONFIG.scenes.forEach((s, i) => {
        if (!(st.phase === 'hub' || st.active === i || inTransit)) return;
        const target = (st.active === i || st.hover === i) ? 1 : .45; this.k[i] += (target - this.k[i]) * .03;
        const c = ctxOf(this.cv[i]); if (c) this.drawScene(i, c, 960, 540, t, this.k[i], dens);
      });
    }
    const dc = ctxOf(this.r.dust); if (dc) this.drawDust(dc, 960, 540, t, dens);
  };

  // ---------- drawing helpers ----------
  rng(seed) { let a = seed >>> 0; return () => { a |= 0; a = a + 0x6D2B79F5 | 0; let t = Math.imul(a ^ a >>> 15, 1 | a); t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t; return ((t ^ t >>> 14) >>> 0) / 4294967296; }; }
  mk(key, n, f) { if (!this.parts[key]) { const r = this.rng(n * 31 + key.length * 977); this.parts[key] = Array.from({ length: n }, () => f(r)); } return this.parts[key]; }
  hex(h) { return [parseInt(h.slice(1, 3), 16), parseInt(h.slice(3, 5), 16), parseInt(h.slice(5, 7), 16)]; }
  mix(a, b, t, al) { t = Math.max(0, Math.min(1, t)); const A = typeof a === 'string' ? this.hex(a) : a, B = typeof b === 'string' ? this.hex(b) : b; const c = A.map((v, i) => Math.round(v + (B[i] - v) * t)); return al == null ? `rgb(${c.join(',')})` : `rgba(${c.join(',')},${al})`; }
  wrap(v) { return ((v % 1) + 1) % 1; }

  drawScene(i, ctx, w, h, t, k, dens) { [this.drawScarlet, this.drawGold, this.drawStar, this.drawMountain, this.drawMeadow][i].call(this, ctx, w, h, t, k, dens); }

  drawScarlet(ctx, w, h, t, k, dens) {
    const P = this.mk('snow', 170, r => ({ x: r(), y: r(), s: .6 + r() * 1.8, v: .02 + r() * .05, ph: r() * 6.28, sw: .2 + r() * .5 }));
    const p = .5 - .5 * Math.cos(t * Math.PI * 2 / 44), wash = Math.min(1, Math.max(0, (p - .12) / .72)) * (.62 + .38 * Math.max(0, (k - .45) / .55));
    const g = ctx.createLinearGradient(0, 0, 0, h);
    g.addColorStop(0, this.mix([64, 6, 18], [236, 232, 226], Math.min(1, wash * 1.3)));
    g.addColorStop(.5, this.mix([122, 18, 38], [245, 241, 235], wash));
    g.addColorStop(1, this.mix([158, 30, 48], [255, 255, 255], wash * 1.2 - .2));
    ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const rg = ctx.createRadialGradient(w * .5, h * .38, 0, w * .5, h * .38, w * .7);
    rg.addColorStop(0, `rgba(255,236,236,${.14 + .1 * wash})`); rg.addColorStop(1, 'rgba(0,0,0,0)'); ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    const n = Math.floor(P.length * Math.min(1, dens * (.5 + .5 * k)));
    for (let j = 0; j < n; j++) {
      const a = P[j], y = this.wrap(a.y + t * a.v * (.6 + a.s * .3)) * h * 1.1 - h * .05, x = this.wrap(a.x + Math.sin(t * a.sw + a.ph) * .02 + t * .004) * w;
      const al = (.55 + .45 * Math.sin(t * 1.3 + a.ph)) * (1 - wash * .5);
      ctx.fillStyle = `rgba(255,255,255,${al * .9})`; ctx.beginPath(); ctx.arc(x, y, a.s * (.8 + .6 * k), 0, 6.283); ctx.fill();
    }
  }
  drawGold(ctx, w, h, t, k, dens) {
    const P = this.mk('ember', 120, r => ({ x: r(), y: r(), s: .7 + r() * 2, v: .03 + r() * .07, ph: r() * 6.28, sw: .3 + r() * .8 }));
    const g = ctx.createLinearGradient(0, 0, 0, h); g.addColorStop(0, '#170c04'); g.addColorStop(1, '#4e2d0e'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const rg = ctx.createRadialGradient(w * .5, h * 1.1, 10, w * .5, h * 1.1, h * 1.4);
    rg.addColorStop(0, 'rgba(255,220,145,.95)'); rg.addColorStop(.28, 'rgba(204,140,50,.78)'); rg.addColorStop(.65, 'rgba(96,56,18,.4)'); rg.addColorStop(1, 'rgba(26,13,4,0)');
    ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    for (let i = 0; i < 3; i++) {
      const x = w * (.25 + .25 * i) + Math.sin(t * .09 + i * 2) * w * .1, y = h * (.55 + .12 * Math.sin(t * .07 + i * 1.7));
      const hz = ctx.createRadialGradient(x, y, 0, x, y, h * .5); hz.addColorStop(0, 'rgba(255,200,120,.09)'); hz.addColorStop(1, 'rgba(255,200,120,0)'); ctx.fillStyle = hz; ctx.fillRect(0, 0, w, h);
    }
    const n = Math.floor(P.length * Math.min(1, dens * (.45 + .55 * k)));
    for (let j = 0; j < n; j++) {
      const a = P[j], yy = 1 - this.wrap(a.y + t * a.v), x = this.wrap(a.x + Math.sin(t * a.sw + a.ph) * .03) * w, y = yy * h;
      const fl = .35 + .65 * Math.abs(Math.sin(t * 3 + a.ph)), al = fl * Math.min(1, yy * 1.6) * (.6 + .4 * k);
      ctx.fillStyle = `rgba(255,150,60,${al * .25})`; ctx.beginPath(); ctx.arc(x, y, a.s * 3, 0, 6.283); ctx.fill();
      ctx.fillStyle = `rgba(255,205,120,${al})`; ctx.beginPath(); ctx.arc(x, y, a.s, 0, 6.283); ctx.fill();
    }
  }
  drawStar(ctx, w, h, t, k, dens) {
    const S = this.mk('stars', 170, r => ({ x: r(), y: r() * .92, s: .3 + r() * 1.2, ph: r() * 6.28, sp: .5 + r() * 1.5 }));
    const g = ctx.createLinearGradient(0, 0, 0, h); g.addColorStop(0, '#03061a'); g.addColorStop(.7, '#0a1540'); g.addColorStop(1, '#16265a'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const n = Math.floor(S.length * Math.min(1, dens));
    for (let j = 0; j < n; j++) { const s = S[j]; ctx.fillStyle = `rgba(255,250,235,${.25 + .75 * (.5 + .5 * Math.sin(t * s.sp + s.ph))})`; ctx.fillRect(s.x * w, s.y * h, s.s, s.s); }
    const sx = w * .5, sy = h * .3, pulse = .85 + .15 * Math.sin(t * .8), br = .55 + .45 * k;
    ctx.globalCompositeOperation = 'lighter';
    const rg = ctx.createRadialGradient(sx, sy, 0, sx, sy, h * .34 * pulse * br);
    rg.addColorStop(0, 'rgba(255,250,232,.95)'); rg.addColorStop(.07, 'rgba(255,240,205,.6)'); rg.addColorStop(.35, 'rgba(180,200,255,.16)'); rg.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    ctx.save(); ctx.translate(sx, sy); const L = h * .98;
    for (let i = 0; i < 9; i++) {
      const ang = Math.PI * .22 + i * (Math.PI * .56 / 8) + Math.sin(t * .25 + i) * .035;
      ctx.save(); ctx.rotate(ang - Math.PI / 2);
      const rg2 = ctx.createLinearGradient(0, 0, 0, L); rg2.addColorStop(0, `rgba(255,236,196,${.13 * br * (.6 + .4 * Math.sin(t * .6 + i * 1.7))})`); rg2.addColorStop(1, 'rgba(255,236,196,0)');
      ctx.fillStyle = rg2; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(-L * .055, L); ctx.lineTo(L * .055, L); ctx.closePath(); ctx.fill(); ctx.restore();
    }
    ctx.strokeStyle = `rgba(255,250,235,${.5 * br})`; ctx.lineWidth = 1;
    for (let i = 0; i < 4; i++) { ctx.save(); ctx.rotate(i * Math.PI / 4 + Math.PI / 8); ctx.beginPath(); ctx.moveTo(0, -h * .07 * pulse); ctx.lineTo(0, h * .07 * pulse); ctx.stroke(); ctx.restore(); }
    ctx.fillStyle = 'rgba(255,255,248,1)'; ctx.beginPath(); ctx.arc(0, 0, 3.2 * pulse, 0, 6.283); ctx.fill();
    ctx.restore(); ctx.globalCompositeOperation = 'source-over';
  }
  drawMountain(ctx, w, h, t, k, dens) {
    const far = [[-.05, .7], [.08, .6], [.18, .64], [.28, .52], [.36, .58], [.46, .5], [.58, .56], [.68, .48], [.78, .55], [.88, .5], [1.05, .6]];
    const mid = [[-.05, .8], [.05, .7], [.14, .62], [.22, .68], [.3, .58], [.38, .64], [.47, .55], [.55, .6], [.64, .5], [.72, .58], [.8, .52], [.9, .62], [1.05, .66]];
    const near = [[-.05, .95], [0, .86], [.1, .8], [.18, .72], [.26, .78], [.34, .68], [.42, .56], [.5, .4], [.56, .5], [.63, .62], [.72, .7], [.8, .66], [.88, .76], [.96, .82], [1.05, .88]];
    const sp = .5 + .5 * Math.sin(t * Math.PI * 2 / 70 - Math.PI / 2), sunY = h * (.68 - .24 * sp);
    const g = ctx.createLinearGradient(0, 0, 0, h);
    g.addColorStop(0, '#141a45'); g.addColorStop(.36, this.mix('#3b2a5e', '#6b3f66', sp)); g.addColorStop(.62, this.mix('#b25a5e', '#e08a55', sp)); g.addColorStop(.8, this.mix('#e8a060', '#f7c877', sp)); g.addColorStop(1, '#fbe0a0');
    ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const rg = ctx.createRadialGradient(w * .5, sunY, 0, w * .5, sunY, h * .5); rg.addColorStop(0, 'rgba(255,230,170,.9)'); rg.addColorStop(.3, 'rgba(255,190,110,.3)'); rg.addColorStop(1, 'rgba(255,190,110,0)');
    ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    ctx.fillStyle = 'rgba(255,246,220,1)'; ctx.beginPath(); ctx.arc(w * .5, sunY, h * .045, 0, 6.283); ctx.fill();
    const ridge = (pts, fill) => { ctx.beginPath(); ctx.moveTo(pts[0][0] * w, pts[0][1] * h); for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0] * w, pts[i][1] * h); ctx.lineTo(w * 1.05, h * 1.1); ctx.lineTo(-w * .05, h * 1.1); ctx.closePath(); ctx.fillStyle = fill; ctx.fill(); };
    ridge(far, this.mix('#6a3f6b', '#8a5a7a', sp)); ridge(mid, '#33254f');
    const lit = ctx.createLinearGradient(0, h * .4, 0, h * (.46 + .5 * sp));
    lit.addColorStop(0, this.mix('#4a3358', '#c98a5e', .3 + .7 * sp)); lit.addColorStop(1, '#141531');
    ridge(near, lit);
    ctx.strokeStyle = `rgba(255,200,120,${.25 + .55 * sp})`; ctx.lineWidth = 1.2; ctx.beginPath(); ctx.moveTo(near[0][0] * w, near[0][1] * h); for (let i = 1; i < near.length; i++) ctx.lineTo(near[i][0] * w, near[i][1] * h); ctx.stroke();
    const px = w * .5, py = h * .4, ph = h * .075; ctx.strokeStyle = '#f3dc9a'; ctx.lineWidth = 1.5; ctx.beginPath(); ctx.moveTo(px, py); ctx.lineTo(px, py - ph); ctx.stroke();
    const fl = w * .04, wave = Math.sin(t * 5) * h * .008, wave2 = Math.sin(t * 5 + 1.5) * h * .006;
    ctx.fillStyle = '#e8c26a'; ctx.beginPath(); ctx.moveTo(px, py - ph); ctx.quadraticCurveTo(px + fl * .5, py - ph + wave2, px + fl, py - ph + h * .012 + wave); ctx.quadraticCurveTo(px + fl * .5, py - ph + h * .02 + wave2, px, py - ph + h * .03); ctx.closePath(); ctx.fill();
    const hz = ctx.createLinearGradient(0, h * .72, 0, h); hz.addColorStop(0, 'rgba(255,220,180,0)'); hz.addColorStop(1, 'rgba(255,220,180,.22)'); ctx.fillStyle = hz; ctx.fillRect(0, h * .72, w, h * .28);
  }
  drawMeadow(ctx, w, h, t, k, dens) {
    const M = this.mk('mist', 9, r => ({ x: r(), y: .5 + r() * .45, rx: .22 + r() * .28, ry: .06 + r() * .08, v: .004 + r() * .008, ph: r() * 6.28 }));
    const F = this.mk('fly', 60, r => ({ x: r(), y: .3 + r() * .65, s: .6 + r() * 1.4, ph: r() * 6.28, v: .004 + r() * .01 }));
    const g = ctx.createLinearGradient(0, 0, 0, h); g.addColorStop(0, '#a9c6a4'); g.addColorStop(.35, '#7fae7a'); g.addColorStop(.6, '#4b8a55'); g.addColorStop(.8, '#2f6b3c'); g.addColorStop(1, '#1d4a2b'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const rg = ctx.createRadialGradient(w * .25, h * .02, 0, w * .25, h * .02, h * .9); rg.addColorStop(0, 'rgba(255,246,205,.5)'); rg.addColorStop(1, 'rgba(255,246,205,0)'); ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    ctx.globalCompositeOperation = 'lighter';
    for (let i = 0; i < 3; i++) {
      const x0 = w * (.12 + .3 * i) + Math.sin(t * .05 + i * 2) * w * .05, sw = w * .09, al = .05 + .03 * Math.sin(t * .4 + i);
      const sg = ctx.createLinearGradient(0, 0, 0, h); sg.addColorStop(0, `rgba(255,245,200,${al * 1.4})`); sg.addColorStop(1, 'rgba(255,245,200,0)');
      ctx.fillStyle = sg; ctx.beginPath(); ctx.moveTo(x0, 0); ctx.lineTo(x0 + sw, 0); ctx.lineTo(x0 + sw + w * .28, h); ctx.lineTo(x0 + w * .28 - sw * .5, h); ctx.closePath(); ctx.fill();
    }
    ctx.globalCompositeOperation = 'source-over';
    for (const a of M) {
      const cx = this.wrap(a.x + t * a.v) * w * 1.3 - w * .15, cy = a.y * h + Math.sin(t * .2 + a.ph) * h * .01;
      ctx.save(); ctx.translate(cx, cy); ctx.scale(a.rx * w, a.ry * h);
      const mg = ctx.createRadialGradient(0, 0, 0, 0, 0, 1); mg.addColorStop(0, `rgba(240,250,235,${.2 + .06 * k})`); mg.addColorStop(1, 'rgba(240,250,235,0)'); ctx.fillStyle = mg; ctx.fillRect(-1, -1, 2, 2); ctx.restore();
    }
    const n = Math.floor(F.length * Math.min(1, dens * (.4 + .6 * k)));
    for (let j = 0; j < n; j++) {
      const a = F[j], x = this.wrap(a.x + t * a.v + Math.sin(t * .4 + a.ph) * .01) * w, y = a.y * h + Math.sin(t * .6 + a.ph) * h * .02;
      ctx.fillStyle = `rgba(255,240,180,${.25 + .6 * (.5 + .5 * Math.sin(t * 1.5 + a.ph))})`; ctx.beginPath(); ctx.arc(x, y, a.s, 0, 6.283); ctx.fill();
    }
  }
  drawSky(ctx, w, h, t, gold) {
    const S = this.mk('skystars', 140, r => ({ x: r(), y: r() * .6, s: .4 + r() * 1.1, ph: r() * 6.28 }));
    const rise = .5 - .5 * Math.cos(Math.min(1, t / 45) * Math.PI);
    const dawn = ['#050a22', '#1a2450', '#6a4470', '#d17a5c', '#f4c37a'], sun = ['#1b1230', '#5a2c3c', '#b8623b', '#ec9a44', '#ffd98a'];
    const g = ctx.createLinearGradient(0, 0, 0, h); [0, .38, .64, .84, 1].forEach((s, i) => g.addColorStop(s, this.mix(dawn[i], sun[i], gold)));
    ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const sa = (1 - rise * .7) * (1 - gold * .6);
    for (const s of S) { ctx.fillStyle = `rgba(255,250,235,${(.3 + .7 * (.5 + .5 * Math.sin(t * 1.2 + s.ph))) * sa})`; ctx.fillRect(s.x * w, s.y * h, s.s, s.s); }
    const sy = h * (1.02 - .2 * rise - .14 * gold); const rg = ctx.createRadialGradient(w * .5, sy, 0, w * .5, sy, h * .9);
    rg.addColorStop(0, 'rgba(255,236,190,.92)'); rg.addColorStop(.16, 'rgba(255,200,130,.55)'); rg.addColorStop(.5, 'rgba(230,140,90,.18)'); rg.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h);
    const hz = ctx.createLinearGradient(0, h * .7, 0, h); hz.addColorStop(0, 'rgba(255,220,170,0)'); hz.addColorStop(1, `rgba(255,225,180,${.25 + .2 * gold})`); ctx.fillStyle = hz; ctx.fillRect(0, h * .7, w, h * .3);
  }
  drawDust(ctx, w, h, t, dens) {
    const P = this.mk('dust', 80, r => ({ x: r(), y: r(), s: .6 + r() * 1.5, vx: (r() - .5) * .006, vy: -.003 - r() * .006, ph: r() * 6.28 }));
    ctx.clearRect(0, 0, w, h); const n = Math.floor(P.length * Math.min(1, dens));
    for (let j = 0; j < n; j++) {
      const a = P[j], x = this.wrap(a.x + t * a.vx + Math.sin(t * .3 + a.ph) * .01) * w, y = this.wrap(a.y + t * a.vy) * h;
      ctx.fillStyle = `rgba(255,236,190,${.1 + .3 * (.5 + .5 * Math.sin(t * .9 + a.ph))})`; ctx.beginPath(); ctx.arc(x, y, a.s, 0, 6.283); ctx.fill();
    }
  }

  // ---------- template values ----------
  renderVals() {
    const C = Component.CONFIG, G = Component.GEOM, st = this.state, D = this.D;
    const poly = pts => `polygon(${pts.map(p => p[0] + 'px ' + p[1] + 'px').join(',')})`;
    const ease = 'cubic-bezier(.7,0,.18,1)', soft = 'cubic-bezier(.22,.8,.2,1)';
    const opening = st.phase === 'opening', hub = st.phase === 'hub', scene = st.phase === 'scene';

    const wedges = C.scenes.map((s, i) => {
      const g = G[i], isA = st.active === i, isH = hub && st.hover === i && !st.inTransit, a = g.angle * Math.PI / 180;
      let clip = poly(g.clip), transform = 'scale(1)', filter = 'none', opacity = 1, z = 1, transition, tint = 1;
      if (opening) { transform = 'scale(0)'; opacity = 0; transition = 'none'; }
      else if (scene) {
        if (isA) { clip = poly(g.full); z = 3; tint = 0; }
        else { transform = `translate(${(Math.cos(a) * 560).toFixed(0)}px,${(Math.sin(a) * 560).toFixed(0)}px) scale(1.12)`; filter = 'blur(16px) brightness(.2)'; opacity = 0; }
        transition = `clip-path ${D}s ${ease}, transform ${D}s ${ease}, filter ${D}s ease, opacity ${D * .7}s ease ${D * .3}s`;
      } else {
        transform = isH ? 'scale(1.045)' : 'scale(1)';
        filter = (st.hover != null && !isH && !st.inTransit) ? 'brightness(.5) saturate(.4)' : 'brightness(1) saturate(1)';
        z = isH ? 2 : 1;
        const tt = !st.assembled ? `${D}s ${soft} ${(i * .1).toFixed(2)}s` : st.inTransit ? `${D}s ${ease}` : `.7s ${soft}`;
        transition = `clip-path ${D}s ${ease}, transform ${tt}, filter .7s ease, opacity ${st.assembled ? '.8s' : D + 's'} ease`;
      }
      const visited = st.visited.includes(i);
      return {
        num: '0' + (i + 1), title: s.title, reference: s.reference, canvasRef: this.cv[i], clip, transform, filter, opacity, z, transition, tint, lx: g.lx, ly: g.ly,
        labelOpacity: hub ? 1 : 0, points: g.clip.map(p => p.join(',')).join(' '),
        rimOpacity: isH ? 1 : visited ? .42 : 0, rimDash: (isH || visited) ? 0 : 3400, rimWidth: isH ? 3 : 1.6,
        onClick: () => { const s2 = this.state; if (s2.phase === 'hub' && !s2.inTransit) this.enterScene(i); },
        onEnter: () => { if (this.state.phase === 'hub') this.setState({ hover: i }); },
        onLeave: () => { if (this.state.hover === i) this.setState({ hover: null }); }
      };
    });

    const cur = C.scenes[st.active ?? this.lastActive] || C.scenes[0];
    const uiOn = scene && st.sceneUI;
    const qs = cur.questions, n = qs.length, W = n > 3 ? 420 : 520, gap = n > 3 ? 30 : 40, x0 = (1920 - (n * W + (n - 1) * gap)) / 2;
    const stagger = st.qReady && !st.qRisen, dset = new Set(st.discussed[st.active] || []);
    const questions = qs.map((q, j) => {
      const x = x0 + j * (W + gap), y = 430 + 90 * Math.sin(Math.PI * (j + .5) / n);
      const focused = st.focusQ === j, other = st.focusQ != null && !focused, done = dset.has(j);
      let transform = `translate(${x}px,${y.toFixed(0)}px)`, opacity = 1, filter = 'none', z = 1;
      if (!st.qReady) { transform = `translate(${x}px,${(y + 90).toFixed(0)}px)`; opacity = 0; }
      else if (focused) { transform = `translate(${960 - W / 2}px,400px) scale(1.22)`; z = 5; }
      else if (other) { transform += ' scale(.86)'; opacity = .28; filter = 'blur(2px)'; }
      else if (done) opacity = .55;
      const dl = stagger ? (j * .13).toFixed(2) : 0;
      return { text: q, w: W, transform, opacity, filter, z, transition: `transform ${stagger ? 1.1 : .9}s ${ease} ${dl}s, opacity .8s ease ${dl}s, filter .8s ease`, checkOpacity: done && !focused ? 1 : 0, onClick: () => this.toggleQ(j) };
    });

    const pOpen = scene && st.panel != null;
    if (pOpen) this.lastPanel = st.panel === 'today' && cur.today ? cur.today : cur.story;
    const pc = this.lastPanel || cur.story;
    const panelParas = pc.paragraphs.map((p, j) => ({ text: p, opacity: pOpen ? 1 : 0, transition: `opacity .9s ease ${pOpen ? (.35 + j * .4).toFixed(2) : 0}s` }));

    const mm = Math.floor(st.timerLeft / 60), ss = st.timerLeft % 60;
    return {
      viewportRef: this.r.viewport, stageRef: this.r.stage, wheelRef: this.r.wheel, bloomRef: this.r.bloom, grainRef: this.r.grain, skyRef: this.r.sky, dustRef: this.r.dust, closeSkyRef: this.r.closeSky,
      wedges, hubTitle: C.hub, mainTitle: C.title,
      skyOpacity: opening ? 1 : 0, bloomOpacity: hub && !st.inTransit ? 1 : 0,
      linesOpacity: hub ? 1 : 0, linesTransition: `opacity .9s ease ${hub ? (D * .7).toFixed(2) : 0}s`,
      centerTransform: opening ? 'translate(-50%,-50%) scale(0)' : scene ? 'translate(-50%,-50%) translate(790px,-436px) scale(.3)' : 'translate(-50%,-50%) scale(1)',
      centerOpacity: opening ? 0 : 1, onHome: () => { if (this.state.phase === 'scene') this.exitScene(); },
      openingTransform: opening ? 'scale(1)' : 'scale(1.7)', openingOpacity: opening ? 1 : 0, openingPointer: opening ? 'auto' : 'none',
      openOn: st.openOn, openTitleY: st.openOn ? 0 : 30, openHintOn: opening ? st.openHint : 0, onStart: () => this.goHub(),
      sceneOpacity: uiOn ? 1 : 0, scenePointer: uiOn ? 'auto' : 'none', sceneTransition: `opacity ${uiOn ? .9 : .45}s ease`,
      sceneTitle: cur.title, sceneRef: cur.reference, sceneScripture: cur.scripture, sceneScriptureRef: cur.scriptureRef,
      bigTitleOpacity: scene && st.titleBig && st.sceneUI ? 1 : 0,
      bigTitleTransform: !st.sceneUI ? 'translate(-50%,-50%) scale(1.15)' : st.titleBig ? 'translate(-50%,-50%) scale(1)' : 'translate(-50%,-50%) scale(.45) translate(-760px,-640px)',
      smallTitleOpacity: scene && st.sceneUI && !st.titleBig ? 1 : 0, smallTitleTransform: (st.titleBig || !scene) ? 'translate(30px,26px) scale(1.4)' : 'translate(0,0) scale(1)',
      scriptureOpacity: scene && st.qReady ? 1 : 0, scriptureTransform: st.qReady ? 'translateY(0)' : 'translateY(-34px)',
      questions,
      onStory: () => this.togglePanel('story'), onToday: () => this.togglePanel('today'),
      storyBorder: st.panel === 'story' ? '#f3dc9a' : 'rgba(217,180,95,.7)', storyGlow: st.panel === 'story' ? .6 : .25,
      todayBorder: st.panel === 'today' ? '#f3dc9a' : 'rgba(217,180,95,.7)', todayGlow: st.panel === 'today' ? .6 : .25,
      todayDisplay: cur.today ? 'flex' : 'none',
      panelTransform: pOpen ? 'translateY(0)' : 'translateY(104%)', panelKicker: this.lastPanel && this.lastPanel !== cur.story ? 'Today' : 'Story',
      panelHeading: pc.heading, panelParas, panelHasImage: !!pc.image, panelNoImage: !pc.image, panelImage: pc.image || '', panelImageLabel: pc.imageLabel || 'drop image here',
      notesOpacity: scene && st.notes ? 1 : 0, notesTransform: st.notes ? 'translateY(0)' : 'translateY(16px)', notesText: cur.note,
      timerOpacity: st.timerOn ? 1 : 0, timerText: `${mm}:${ss < 10 ? '0' : ''}${ss}`, timerColor: st.timerOn && st.timerLeft <= 30 ? '#ffb37a' : '#e8d8ab',
      closingOpacity: st.closing ? 1 : 0, closingPointer: st.closing ? 'auto' : 'none',
      blackoutOpacity: st.blackout ? 1 : 0, blackoutPointer: st.blackout ? 'auto' : 'none',
      hintOpacity: st.hint, grainOpacity: (this.props.grain ?? true) ? .07 : 0
    };
  }
}
</script>
</body>
</html>
